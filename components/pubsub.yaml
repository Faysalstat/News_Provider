apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: pubsub  # This is the name you should use in your application
  namespace: default
spec:
  type: pubsub.redis
  version: v1
  metadata:
  - name: redisHost
    value: "localhost:6379"
  - name: redisPassword
    value: ""

